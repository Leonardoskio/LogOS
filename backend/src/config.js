/**
 * Central backend configuration.
 *
 * This module will read environment variables such as PORT,
 * VAULT_TEMPLATE_PATH, ACTIVE_VAULT_PATH, AI_PROVIDER and LOGOS_TIMEZONE.
 * Keeping configuration here prevents paths and provider names from being
 * duplicated across routes and services.
 */
