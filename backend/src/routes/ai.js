/**
 * AI routes.
 *
 * Planned endpoints:
 * - POST /api/ai/report-giornaliero
 * - POST /api/ai/consigli-logistici
 *
 * AI routes read structured data already validated by the backend and delegate
 * prompt/provider execution to aiService.
 */
