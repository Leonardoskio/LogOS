/**
 * Tractor routes.
 *
 * Planned endpoints:
 * - GET /api/motrici
 * - GET /api/motrici/:id
 * - POST /api/motrici
 * - PATCH /api/motrici/:id/stato
 *
 * Frequent real-time updates should not be written directly to Markdown.
 */
