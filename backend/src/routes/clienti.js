/**
 * Customer routes.
 *
 * Planned endpoints:
 * - GET /api/clienti
 * - GET /api/clienti/:id
 * - POST /api/clienti
 *
 * Customer shipment counts are derived from shipment notes, not manually stored
 * as primary data.
 */
