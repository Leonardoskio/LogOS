/**
 * Shipment routes.
 *
 * Planned endpoints:
 * - POST /api/spedizioni
 * - GET /api/spedizioni
 * - GET /api/spedizioni/:id
 *
 * These routes receive structured form data, validate request shape and call
 * archive/vault services. They should not write Markdown directly.
 */
