/**
 * Vault service.
 *
 * Responsible for reading templates, rendering Markdown and writing notes into
 * the active company vault. This service should be the only layer that knows
 * about Obsidian vault paths and file naming details.
 */
