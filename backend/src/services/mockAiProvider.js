/**
 * Mock AI provider.
 *
 * Used during development so frontend and backend flows can be tested before a
 * real AI provider is configured. The mock should return deterministic Markdown
 * responses based on the input shape.
 */
