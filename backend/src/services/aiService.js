/**
 * AI service.
 *
 * Responsible for loading prompt templates, preparing structured inputs and
 * delegating generation to the selected provider. It should not decide archive
 * paths, generate shipment IDs or mutate primary logistics data.
 */
