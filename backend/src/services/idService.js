/**
 * ID service.
 *
 * Responsible for generating stable IDs such as SP-2026-00001. For the MVP this
 * can use a counter file under the runtime vault; later it can move to a
 * database sequence without changing route contracts.
 */
