/**
 * Backend entry point.
 *
 * This file will create the Node.js/Express server, load configuration, attach
 * API routes and start listening on PORT. It should not contain business logic:
 * route files handle HTTP boundaries and services handle vault/AI operations.
 */
